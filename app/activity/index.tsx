import { ComingSoon } from "@/screens/ComingSoon";

/*
  Placeholder Activity route so the Chats inbox "Reactions" cell (and
  Profile's Activity hotlink) navigate to a real screen. Replaced by the
  real ActivityDefault when the Activity section is built.
*/
export default function Activity() {
  return <ComingSoon section="Activity" />;
}
